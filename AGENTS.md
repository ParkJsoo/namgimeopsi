# 남김없이 작업 가이드

## 프로젝트 목적

`남김없이`는 출시 우선 프로젝트가 아니라, AI 신뢰 UX와 모바일 제품 구현 역량을 보여 주는 공개 포트폴리오 프로젝트다. 기능 수를 늘리기보다 아래 세 흐름을 실제 데이터로 끝까지 완성한다.

1. 영수증 촬영 → AI 품목 초안 → 사용자 검수 → 재고 입고
2. 남은 음식 빠른 등록 → 권장 섭취 시점 관리
3. 오늘의 메뉴 선택 → 조리/섭취 완료 → 재료 수량 차감

## 문서 우선순위

작업 전에는 다음 문서를 읽고, 서로 다를 경우 위에서 아래 순서로 따른다.

1. `docs/00-current-context.md` — 현재 상태, 직전 결정, 다음 작업
2. `docs/01-product-brief.md` — 제품 가설과 MVP 범위
3. `docs/02-ux-specification.md` — 사용자 흐름과 상태
4. `docs/03-technical-design.md` — 데이터 모델과 기술 설계
5. `docs/06-design-direction.md`, `docs/07-wireframe-specification.md` — UI 기준

## 제품 원칙

- AI는 재고를 자동 확정하지 않는다. 항상 `확인 / 수정 / 제외`가 가능한 초안이어야 한다.
- 낮은 신뢰도는 숨기지 않고 `확인 필요`로 드러낸다.
- 날짜의 의미를 구분한다. 포장 표기일, 구매일, 조리/보관 시작일, 권장 섭취 시점을 혼용하지 않는다.
- 추천은 최대 3개이며, 임박 재료·부족 재료·조리 시간·추천 이유를 설명한다.
- 생활 단위(`반 봉지`, `1모`, `2인분`, `조금 남음`)를 우선한다.

## 범위 규칙

- MVP 밖: 전체 냉장고 사진 인식, 바코드 상품 DB, 가족 공유, 주문 연동, 자유형 AI 요리 챗, 영양/알레르기, 스마트 냉장고 연동.
- 구현 스택의 기본값은 React Native + Expo + TypeScript + Supabase다. 새 라이브러리는 현재 MVP에 필요한 경우에만 추가한다.
- 레시피 추천의 핵심은 결정론적 점수화다. 자유형 LLM 생성으로 핵심 추천 로직을 대체하지 않는다.

## 작업 방식

- 구현 시작 전 현재 워크트리와 기존 문서를 확인한다.
- 변경이 제품 결정이나 다음 작업에 영향을 주면 `docs/00-current-context.md`를 함께 갱신한다.
- 문서와 구현은 실제 동작 여부를 구분해 작성한다. 목업을 구현 완료처럼 표현하지 않는다.
- 관련 변경은 검증 후 작은 단위로 커밋한다.
- 이 저장소 밖의 프로젝트·워크스페이스는 작업 대상이 아니다. 특히 `withnetworks` 관련 파일·Figma 워크스페이스에는 읽기·쓰기·조작을 하지 않는다.

## 디자인 파일

- Figma 파일: `남김없이 — Mobile App Design`
- 개인 워크스페이스 `박정수의 팀`만 사용한다.
- Starter 플랜 제한으로 페이지는 `Cover / Foundations / App Screens` 세 개를 넘기지 않는다.
- 디자인 토큰과 현재 Figma 작업 상태는 `docs/00-current-context.md`에서 먼저 확인한다.
