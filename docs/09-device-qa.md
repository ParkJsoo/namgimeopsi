# 실기기 QA 기록

## iOS 시뮬레이터 Release 후속 QA (2026-09-08)

사용자 요청으로 `Namgimeopsi QA iPhone 13 mini` / iOS 26.5 전용 시뮬레이터에서 진행했다. 시작 HEAD는 `bb3258e`이며 앱 소스는 `cd8b625`와 같다. 기존 앱 위에 최신 Release를 설치했고 재고를 초기화하지 않았다. 아래는 에이전트가 직접 조작·관찰한 **시뮬레이터 결과**다. 실제 iPhone 본체 QA를 대체하지 않는다.

### 입력·저장·차감

| 항목 | 결과와 관찰 |
| --- | --- |
| 영수증 업로드·검수 | 지정된 테스트 영수증 PNG를 시뮬레이터 사진 앱으로 가져와 선택했다. 실제 업로드·fixture 검수 진입과 제공자 미연결 고지를 확인했다. |
| 영수증 한글 입력 | 소프트웨어 한글 키보드의 `ㅋ·ㅏ·ㄹ·ㅔ` 버튼으로 이름 `카레`를 조합했다. 빈 이름일 때 입고 버튼 비활성, 입력 후 활성화를 확인했다. |
| 영수증 마지막 필드·취소 | 접근성 요소로 마지막 권장일 필드에 포커스하자 목록이 이동하고 해당 필드가 키보드 위에 표시됐다. 키보드를 닫은 뒤 `6개 냉장고에 담기`·`아직 저장하지 않을게요` 전체를 확인하고 취소했다. 추가 입고는 확정하지 않았다. |
| 남은 음식 입력·저장 | 이름의 `카레`는 화면 키보드로 조합했고 최종 이름은 `QA SIM 카레`다. 수량 `2인분`은 접근성 입력으로 지정했다. 마지막 권장 시점 `내일까지`가 키보드 위에 보이고 저장 버튼도 표시됐다. 키보드를 닫은 뒤 저장·닫기 전체를 확인하고 저장했다. 목록에 `2인분 · 냉장 보관 시작 지금`이 표시됐다. |
| 조리 입력·확정 | 애호박 두부덮밥에서 두부를 `남은 양`으로 바꿨다. 화면 한글 키보드 자모로 `반 모`를 조합했고 필드·키보드가 함께 보였다. 키보드를 닫은 뒤 `재료 사용 완료`·`아직 있어요` 전체를 확인했다. 두부 부분 소비·애호박 전량 소비를 확정했다. |
| 종료·재실행 유지 | `simctl terminate` 후 다시 실행해 `QA SIM 카레 2인분`, 두부 `반 모`, 활성 목록에서 애호박 제외를 확인했다. 계란 `9개`·남은 치킨 `1인분`은 유지됐다. |
| 저장 원장 대조 | 앱 컨테이너의 재고 캐시에서 lot `tofu`의 `반 모`, `zucchini`의 `consume-all`, 같은 `cooking-1788859014389-2` 세션의 원장 2건을 확인했다. 전량 소비 lot 자체는 보존되고 `getActiveInventoryItems`에서 제외된다. 저장된 outbox는 0건이었다. 원격 DB를 직접 조회한 검증은 아니다. |

**스크롤 범위:** 이번 Computer Use의 휠 스크롤·드래그는 시뮬레이터에서 일관된 목록 이동을 만들지 못했다. 마지막 필드의 접근성 포커스에 따른 자동 스크롤과 키보드 회피·버튼 표시는 확인했지만, 손가락 스와이프·키보드 유지 스크롤을 신규 통과로 기록하지 않는다. 이전 시뮬레이터의 스와이프 관찰 및 실제 본체 QA와 구분한다. Mac 키 입력으로 한글을 전송하는 것은 안정적이지 않아 실제 한글 조합은 소프트웨어 키보드 버튼으로 수행했다.

### 업로드·분석 중 닫기 — 인위적 응답 지연 조건

- 원래 Release에서는 응답이 빨라 업로드 단계 감지 뒤 클릭하는 사이 검수로 전환됐다. 이를 닫기 성공으로 세지 않았다.
- 네이티브 시트에서 누를 시간을 확보하기 위해 `ReceiptEntrySheet.tsx`의 업로드·분석 `await` 직후, `isCurrent()` 검사 전에 각각 15초를 기다리는 임시 시뮬레이터 QA 빌드를 만들었다. 실제 사진 선택·업로드·fixture 분석 호출은 유지했다. 네트워크 요청을 지연하거나 실패시킨 테스트는 아니며 **성공 응답의 UI 전달 지연**이다.
- 업로드 단계의 `닫기` 표시·터치 → 홈 복귀 → 즉시 `+` 재진입 → 15초 이상의 지연 완료 후에도 등록 방식 선택 화면 유지: 통과.
- 분석 단계의 제공자 미연결·fixture 고지와 `닫기` 표시·터치 → 홈 복귀 → 즉시 재진입 → 지연 완료 후에도 등록 방식 선택 화면 유지: 통과.
- 두 경우 후 로컬 재고·원장 전체가 조리 QA 직후 상태와 동일함을 대조했다. 추가 입고는 없고, 업로드 원본·scan job은 삭제하지 않았다. 지연 실패 응답은 이번 시뮬레이터에서 유발하지 않았으며 기존 `test:receipts` 자동 테스트의 범위다.
- 임시 지연은 빌드 직후 원문으로 복원했고 `git diff --exit-code -- src/features/receipts/ReceiptEntrySheet.tsx`를 통과했다. 검증 후 지연 없는 원래 Release를 재빌드·갱신 설치했으며 최초 원래 번들 해시와 일치했다. 최종 실행에서도 저장·차감 상태를 다시 확인했다. 빌드·복원 결과는 [iOS preview 기록](11-ios-preview.md)을 따른다.

이번 화면 증거는 대화의 Simulator 캡처이며 별도 이미지 파일은 저장소에 추가하지 않았다. 테스트 남은 음식과 소비 원장은 보존한다. 실제 iPhone의 세 입력 시트·저장·차감 및 실제 네트워크 타이밍에서의 진행 중 닫기는 여전히 별도 본체 확인 대상이다.

## iPhone Release QA (2026-09-08, `cd8b625`)

- iPhone 13 mini의 실제 OS는 26.6.1이다. USB 통신·페어링·개발자 모드·미러링을 확인했다. 기존 설치는 Metro 부재 시 시작 오류를 보였으며, 버전 번호만으로 기존 설치의 소스 커밋은 확인할 수 없다.
- 최신 코드로 Apple Development 서명된 로컬 Release를 빌드하고 기존 앱 위에 설치했다. Metro 없는 cold launch·기존 냉장 lot 표시를 직접 확인했다. 빌드·서명 만료·재현 명령은 [iOS preview 기록](11-ios-preview.md)을 따른다.
- 미러링에서 지정된 테스트 영수증을 선택해 업로드 → 검수 진입과 제공자 미연결·fixture 고지를 직접 확인했다. 추가 입고는 확정하지 않았다. 개인 사진 썸네일을 저장소에 추가하지 않았다.
- 업로드 단계의 새 `닫기` 표시는 관찰했다. 사진 선택 후 닫기 터치 시에는 이미 검수로 넘어가 있어, 진행 중 닫기 성공으로 기록하지 않는다. 분석 단계의 표시·닫기 및 닫기 후 재진입은 미확인이다.
- 본체 영수증 키보드 확인을 요청한 뒤, 사용자가 시뮬레이터 검증으로 진행하도록 요청했다. 위 후속 QA를 수행했으며 본체 결과 보고는 아직 없다. 남은 음식·조리 시트의 본체 키보드와 저장·부분/전량 차감·재실행 유지는 미완료다. 시뮬레이터·자동 회귀 테스트 성공으로 대체하지 않는다.

## Galaxy 키보드 회피 수정 및 스크롤 확인 (2026-09-08)

- SM-F766N / Android 16에서 최신 로컬 Debug 빌드·설치에 성공했다. 테스트 영수증은 사용자가 기기에 저장했다고 확인했다.
- 남은 음식 등록 시 사용자가 본체에서 입력칸/버튼이 키보드에 가려지고 스크롤도 되지 않는다고 보고했다. 직접 캡처로 확인한 결과와 구분한다.
- 공용 `KeyboardSheet`의 Android `behavior`가 미지정이었던 것을 `height`로 변경했다. 키보드가 차지하는 높이에 맞춰 스크롤 영역을 줄이도록 했으며 iOS의 `padding`은 유지한다.
- `height`만 적용한 뒤 사용자가 버튼 끝만 보이고 스크롤하면 키보드가 닫힌다고 보고했다. 전문 에이전트가 설치된 React Native의 Android ScrollView 구현에서 `on-drag`가 `dismissKeyboard()`를 호출하는 것을 확인했다. Android에만 `keyboardDismissMode="none"`을 적용했고 iOS 동작과 버튼의 `keyboardShouldPersistTaps="handled"`는 유지했다.
- 수정 후 연결된 갤럭시에서 ADB 터치 스와이프와 화면 캡처로 한글 키보드가 열린 채 마지막 권장 시점·`냉장고에 담기`·`닫기` 전체가 보이는 것을 직접 확인했다. 기존 `카레`·`2인분` 값도 유지됐다. 이번에는 저장을 누르지 않았으며 이후 시스템 뒤로가기로 시트가 닫혀 홈으로 복귀했다.
- TypeScript·lint·diff 검사 통과. 사용자도 본체에서 스크롤이 잘 되고 끝까지 내리면 버튼이 잘 보인다고 재확인했다. 남은 음식 시트의 키보드 유지 스크롤·하단 버튼 접근은 통과로 기록한다. 영수증·조리 완료 시트의 후속 확인은 아래에 기록한다. 키보드 재열기 반복은 별도 확인이 남아 있다. 캡처는 임시 파일로만 확인했으며 저장소에 추가하지 않았다.

## Android Release 및 네트워크 후속 검증 (2026-09-08)

에이전트가 직접 수행한 오프라인 실패·복구, 시스템 사진 선택기 권한 범위, Release 설치·Metro 없는 실행, 키보드 회귀 및 캐시/원격 lot 대조는 [Android preview 기록](10-android-preview.md)을 따른다. DNS 오류 원문 노출을 사용자용 재시도 안내로 수정했다. 아래 이전 기록의 preview 미완료 표기는 이 최신 Android 결과로 대체하며, iOS preview·HEIC/용량 경계·접힘 QA는 남아 있다.

## 전문 리뷰 수정·자동 회귀 검증 (2026-09-08)

- 저장 실패 후 동일 영수증 재시도가 잘못 `already-confirmed`가 되는 경로를 수정했다. `npm run test:inventory`는 실제 hook을 실행하고 저장소·네트워크 경계만 대체해 반복 저장 실패, 복구 후 6개 lot/6개 원장·RPC 1회, 원격 실패 후 outbox 재시도 3개 경우를 검증한다.
- 업로드·분석 두 단계에 `닫기`를 추가했다. `npm run test:receipts`의 새 컴포넌트 테스트는 단계별 닫기 후 지연 성공/실패 4개 경우에서 재진입 화면 유지·오래된 응답 차단·재고 확정 미호출을 검증한다. 실제 업로드 중단/원본 삭제를 뜻하지 않는다.
- 기존 도메인·날짜·세션 테스트, TypeScript·lint 통과. 전문 에이전트 2명의 수정 재리뷰에서 추가 차단 이슈 없음. Android Release 빌드·갱신 설치 성공. 저장 공간 부족을 본체에 유발하거나 iPhone의 새 닫기 버튼을 실제 터치한 결과는 아니며, 그 네이티브 확인과 나머지 경계 QA는 구분한다.

## 이전 iPhone Debug QA 환경과 범위

- 코드 기준: `d960a6c` (`fix: allow cancelling inventory editor on iOS`)
- iPhone 13 mini / iOS 26.6 / USB 설치한 로컬 Debug 앱, Mac의 iPhone 미러링으로 조작
- 갤럭시 SM-F766N / Android 16 / 로컬 Debug 앱 설치·실행 확인까지 완료
- 앱 ID: `com.parkjsoo.namgimeopsi`. Metro가 필요한 Debug 실행이며 preview build 검증은 아니다.
- 입력 이미지: `~/Downloads/namgimeopsi-test-receipt.png`를 사용자가 iPhone 사진 앱에 저장했다. 개인정보 없는 테스트 영수증이며, 현재 분석 결과는 이미지 내용과 무관한 고정 fixture다.
- 아래 시각 결과는 이 세션의 미러링 캡처로 확인했다. 별도 증거 이미지 파일이나 영상은 저장하지 않았다. 원격 DB/RPC를 직접 조회하는 E2E는 이번에 재실행하지 않았다.

## Galaxy 영수증 검수 — 사용자 본체 확인 (2026-09-08)

- 사용자가 `+ → 영수증으로 등록`에서 저장된 테스트 영수증을 선택하고 검수 화면에 진입했다.
- 품목 이름 입력으로 키보드를 연 뒤 끝까지 스크롤해 `6개 냉장고에 담기`·`아직 저장하지 않을게요` 버튼이 보이는 것을 확인했다고 보고했다. 영수증 검수의 키보드 스크롤·하단 버튼 접근은 통과다.
- 이번 확인은 사용자 본체 보고이며 별도 캡처는 없다. 입고 확정, 권한 거부/제한 상태, 원격 저장 검증까지 완료했다는 의미는 아니다. 조리 완료 시트 후속 결과는 아래에 기록한다.

## Galaxy 조리 완료 — 사용자 본체 확인 (2026-09-08)

- 사용자가 영수증 검수를 취소하고 홈의 애호박 두부덮밥을 선택한 뒤, 두부의 `남은 양`에 `반 모`를 입력하는 안내를 수행했다고 보고했다.
- 키보드를 연 채 스크롤해 `재료 사용 완료`·`아직 있어요` 버튼이 모두 보이는 것을 확인했다. 사용자 본체 보고 기준으로 조리 시트의 입력·하단 버튼 접근은 통과다. 별도 캡처는 없다.
- 이로써 갤럭시 세 입력 시트의 키보드 유지 스크롤·하단 버튼 접근을 확인했다. 이번에는 소비 확정을 요청하지 않았으며 실제 차감·재시작 후 유지 검증으로 기록하지 않는다. 다음은 남은 음식 저장부터 실제 상태 변경과 재진입 확인이다.

## Galaxy 남은 음식 저장 — 사용자 본체 확인 (2026-09-08)

- 사용자가 조리 시트를 취소한 뒤 남은 음식 `QA 카레`·`2인분`·`냉장`으로 등록하는 안내를 수행했고, 냉장고 목록에 표시된다고 확인했다. 사용자 보고 기준으로 남은 음식 저장·목록 표시는 통과다.
- 사용자가 최근 앱 화면에서 앱을 종료한 뒤 다시 실행했고, 냉장고에 `QA 카레 · 2인분`이 그대로 표시된다고 확인했다. 사용자 본체 보고 기준으로 앱 재실행 후 유지도 통과다.
- 테스트 항목은 유지한다. 별도 캡처·원격 DB 조회는 하지 않았으므로 이 결과만으로 원격 동기화 완료를 입증하지 않는다. 다음은 영수증 검수 확정 입고와 목록 반영 확인이다.

## Galaxy 영수증 확정 입고 — 사용자 본체 확인 (2026-09-08)

- 사용자가 테스트 영수증을 선택하고 돼지고기를 제외한 뒤 `5개 냉장고에 담기`를 눌러 완료 화면과 보관 위치별 추가를 확인하는 절차를 수행했다고 보고했다. 냉장 계란·두부·애호박, 냉동 만두, 실온 참기름의 입고·목록 표시는 사용자 보고 기준 통과다.
- 기존 동명 lot와 테스트 입고 항목은 유지한다. 별도 화면 캡처·원격 DB 조회·동일 batch 재확정 검증은 수행하지 않았다. 다음은 조리 완료의 부분/전량 차감과 재실행 후 유지 확인이다.

## Galaxy 조리 차감·재실행 및 직접 조작 후속 QA (2026-09-08)

- 사용자가 애호박 두부덮밥에서 두부 `반 모` 부분 차감·애호박 `다 먹음` 확정 후 목록 반영을 확인했고, 종료·재실행 후에도 유지된다고 보고했다. 사용자 본체 보고 기준으로 부분/전량 차감·재실행 유지 통과다.
- 에이전트가 ADB 캡처로 현재 냉장 목록의 두부 `반 모`, `QA 카레 2인분`을 직접 확인했다. 다른 lot인 두부 `1모`와 애호박 `반 개`도 남아 있다. 차감 전후 lot ID 대조나 원격 DB 조회는 이번에 수행하지 않았다.
- 에이전트가 ADB 터치로 영수증 사진 선택기를 열고 시스템 뒤로가기로 취소 → 등록 방식 선택 복귀 → 사진 선택기 재진입을 직접 확인했다. 사진을 선택하거나 추가 업로드·입고하지 않았다. 개인 사진 썸네일이 포함된 선택기 캡처는 저장소에 추가하지 않는다.
- 이후 USB 디버깅으로 가능한 화면 확인·터치·재실행은 에이전트가 직접 수행한다. 사용자에게 같은 조작을 반복 요청하지 않는다. 남은 사진 권한 상태별 QA·오류 복구와 독립 실행 preview는 미완료다.

## iPhone에서 확인한 결과

| 항목 | 결과 | 관찰 |
| --- | --- | --- |
| 앱 시작 | 통과 | 개발 앱 신뢰 확인과 로컬 네트워크 허용 뒤 `Reload JS`로 홈 표시 |
| 동기화 재시도 | 통과 | 초기 오프라인 안내를 누른 뒤 안내 해제 |
| 사진 권한 문구 | 통과 | 비공개 저장·검수용 초안이라는 한국어 설정 문구 표시 |
| 사진 접근 거부 | 통과 | `허용 안 함` 뒤 접근 허용 안내와 등록 방식 선택 화면으로 복귀 |
| 제한된 접근 | 통과 | 사용자가 설정에서 테스트 사진만 허용한 상태로 선택·업로드 성공 |
| fixture 고지 | 통과 | 검수 상단의 제공자 미연결·fixture 고지와 하단 `AI 추정 · 확인 필요` 표시 |
| 수량 입력 검증 | 통과 | 수량을 비우면 확정 버튼 비활성, `9` 입력 시 활성화와 표시 반영 |
| 품목 제외 | 통과 | 계란 제외 시 6개 → 5개, `다시 포함` 및 제외 안내 표시 |
| 검수 취소 | 통과 | `아직 저장하지 않을게요`로 홈 복귀. 냉장 목록은 기존 계란 9개·두부 1모·애호박 반 개·남은 치킨 상태로 표시 |
| 새 검수 | 통과 | 동일 테스트 사진을 새로 업로드하자 계란 10개·포함 6개인 새 fixture 초안 표시 |
| 확정 입고 | 통과 | 새 초안에서 돼지고기 제외 후 `5개 냉장고에 담기` → 완료 화면 표시 |
| 보관 위치별 입고 | 통과 | 냉장: 계란 10개·두부 1모·애호박 1개 추가, 냉동: 만두 2봉 추가, 실온: 참기름 1병 추가. 기존 동명 lot는 별도로 유지 |
| 편집 시트 취소 | 수정 후 통과 | 직접 추가/남은 음식 공용 시트에 `닫기` 추가, 저장 없이 홈 복귀 확인 (`d960a6c`) |

이번 iOS 시스템 사진 선택기에는 테스트 영수증 외 사진의 썸네일도 표시됐다. 사용자가 지정한 테스트 영수증만 선택했으며, 이 관찰을 앱이 전체 사진 데이터에 접근했다는 증거로 해석하지 않는다.

검수 취소는 **재고 입고 취소**다. 이미 업로드한 원본·scan job까지 삭제되는지는 이번에 검증하지 않았고, 취소 후 원격 데이터 삭제도 수행하지 않았다. 같은 사진을 새로 업로드한 것은 새 scan job이므로 동일 batch 멱등성 검증으로 기록하지 않는다.

## 남은 검증

- iPhone 본체의 한글 화면 키보드: 입력·조합, 검수/남은 음식/조리 완료 시트의 확정·닫기 버튼 접근. 미러링 입력에서는 화면 키보드가 나타나지 않았고 한글 입력·클립보드 전달이 안정적이지 않아 검증 완료로 간주하지 않는다.
- 사진 선택 취소 후 재진입, 전체 접근, 실제 HEIC·10MB 경계·네트워크 실패 복구
- iPhone 남은 음식 저장, 조리 완료 부분·전량 차감, 재시작 후 상태 유지
- 갤럭시 주요 사진 선택·레이아웃·세 흐름은 후속 QA에서 확인했다. 접힘/펼침·긴 이름·이미지 형식/용량 경계는 남아 있다.
- iOS preview 설치와 Metro 없이 실행 확인. Android 로컬 Release 결과는 `docs/10-android-preview.md`를 따른다.

테스트 확정 입고 5개 lot는 iPhone 데모 계정에 남겨 두었다. 기존 재고나 테스트 원본을 임의로 삭제하지 않는다. 다음 작업은 본체 키보드 QA부터 이어간다.

## 리뷰 수정 후 시뮬레이터 키보드 QA (2026-09-08)

실기기와 분리한 `Namgimeopsi QA iPhone 13 mini` / iOS 26.5 시뮬레이터에서 로컬 Debug 빌드를 실행했다. 세 시트에 공용 `KeyboardSheet`를 적용해 높이를 제한하고 전체 내용과 버튼을 스크롤하게 했다. iOS 키보드 높이에 따라 가용 영역이 줄며, 터치 스와이프는 키보드를 내린다.

| 시트 | 확인 결과 |
| --- | --- |
| 남은 음식/직접 추가 공용 편집 | 소프트웨어 한글 키보드의 자음·모음 버튼으로 `카레` 조합 입력. 마지막 권장 시점 입력란이 키보드 위에 표시됨. 스와이프 후 저장·닫기 접근 및 저장 없이 취소 확인 |
| 조리 완료 | `남은 양` 필드와 소프트웨어 키보드 동시 표시, `반 모` 값 유지. 스와이프 후 `재료 사용 완료`·`아직 있어요` 접근 및 차감 없이 취소 확인 |
| 영수증 검수 | 첫/마지막 권장일 입력란이 소프트웨어 키보드 위에 표시됨. 하단 입고·취소 버튼까지 이동 가능하며 검수 취소로 홈 복귀. `2026-09-31`은 오류·확정 비활성, `2026-09-20`은 오류 해제·확정 활성 확인 |

영수증 PNG의 시뮬레이터 가져오기 작업이 지연돼 중단하고 기본 꽃 샘플 사진으로 fixture 검수를 열었다. 실제 iPhone의 앞선 영수증 업로드 결과와 별도 기록이며, 이 시뮬레이터에서는 입고·소비·남은 음식 저장을 확정하지 않았다. 키보드 QA용 샘플 업로드/scan job은 생성됐으며 삭제하지 않았다. 이번 시각 증거도 대화의 캡처이고 별도 파일로 저장하지 않았다.

회귀 검증: `npm run test:domain`, `npm run test:receipts`, `npx tsc --noEmit`, `npm run lint`, `npx expo export --platform web` 통과. 새 테스트는 날짜 수정→입고→추천 상태, 잘못된 달력 날짜·빈 날짜·제외 항목, 닫힌 요청 A가 새 요청 B보다 늦게 완료되는 경우를 다룬다. 지연 응답 테스트는 요청 세션 단위 테스트이며 Android 본체 네트워크 지연 재현을 대체하지 않는다.

시뮬레이터 결과는 실제 iPhone 본체와 갤럭시의 입력·키보드 QA 완료로 간주하지 않는다. 위 남은 실기기·preview 항목은 유지한다.
